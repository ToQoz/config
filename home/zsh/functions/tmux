emulate -L zsh

# tmux wrapper: with no args, hand off to enter-workspace (repo picker →
# attach/switch tmux session). With any args, forward verbatim to real tmux.

if (( $# > 0 )); then
  command tmux "$@"
  return
fi

enter-workspace
